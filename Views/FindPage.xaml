<Page
    x:Class="MyWork.Views.FindPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyWork.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        
        <StackPanel Orientation="Vertical">
            <TextBlock Name="findTextBlock" Text="Find Product" Margin="10" Width="350"/>
            <TextBox Name="findTextBox" Margin="10" Width="350"/>
            <Button Name="findButton" Content="Find" Click="findButton_Click" Margin="465,10" Width="100"/>
        </StackPanel>
        <ListBox Background="BlanchedAlmond" Grid.Row="1" Name="outputListbox" HorizontalAlignment="Center" Width="400"/>
    </Grid>
</Page>
